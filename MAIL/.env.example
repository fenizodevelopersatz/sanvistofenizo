# Copy to .env (gitignored) and fill in real values. Never commit .env.
SMTP_HOST=smtp.hostinger.com
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_PORT=465

# Inbox that should receive contact-form / demo-request leads.
RECIPIENT_EMAIL=
RECIPIENT_NAME=CloneScript Team
